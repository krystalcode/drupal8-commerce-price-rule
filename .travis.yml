language: php

php:
  - 5.5.9
  - 5.6
  - 7
  - 7.1

install:
  - composer self-update
  - composer install --prefer-source --no-interaction

script:
  # Run the PHPUnit tests
  ./vendor/phpunit/phpunit/phpunit -c ./core/phpunit.xml.dist --verbose ./modules/commerce_price_rule
